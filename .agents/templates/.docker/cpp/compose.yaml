services:
  app:
    build:
      context: ../
      dockerfile: .docker/Dockerfile.cpp
      target: dev
    volumes:
      - ../:/app
    working_dir: /app
    stdin_open: true
    tty: true
    env_file:
      - ../.env
    environment:
      - CPP_ENV=development
