services:
  app:
    build:
      context: ../
      dockerfile: .docker/Dockerfile.java
      target: dev
    volumes:
      - ../:/app
    working_dir: /app
    stdin_open: true
    tty: true
    env_file:
      - ../.env
    environment:
      - JAVA_ENV=development
