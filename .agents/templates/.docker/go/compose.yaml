services:
  app:
    build:
      context: ../
      dockerfile: .docker/Dockerfile.go
      target: dev
    ports:
      - "8080:8080"
    volumes:
      - ../:/app
    working_dir: /app
    stdin_open: true
    tty: true
    env_file:
      - ../.env
    environment:
      - GO_ENV=development
